
# Copy to .env and fill in values before running

# When running producer locally (outside Docker): localhost:9092
# When running producer inside Docker network:    kafka:29092
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

KAFKA_TOPIC=banking-transactions

# Path to the downloaded Kaggle CSV
CSV_FILE_PATH=data/transactions.csv

# Events flushed to Kafka per batch
PRODUCER_BATCH_SIZE=500

PIPELINE_VERSION=1.0.0

# Registered schema_id (must have a matching file under ingestion/src/main/resources/schemas/)
SCHEMA_ID=banking_transaction_v1

# Payload field used as the per-row unique key (trade_id / dedup key)
ID_FIELD=transaction_id

# EventEnvelope.event_type label
EVENT_TYPE=banking_transaction
